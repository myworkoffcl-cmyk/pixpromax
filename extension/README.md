# PixProMax Quick Tools

This is a Manifest V3 browser-extension starter that opens PixProMax tools quickly. It intentionally does not read page contents, upload files, or process files itself.

To test it in Chromium-based browsers, open the extensions page, enable Developer mode, choose **Load unpacked**, and select this `extension` folder. Add the icon PNGs before packaging for a browser store.
