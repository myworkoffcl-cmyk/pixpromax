import type { Metadata } from "next";
import { tools } from "@/config/tools";
export const metadata: Metadata = { title: "About", description: "Learn about PixProMax and its practical browser-based image and PDF tools.", alternates: { canonical: "/about" } };
export default function AboutPage() { const categories = [...new Set(tools.filter((tool) => tool.status === "active").map((tool) => tool.category))]; return <article className="legal-page shell"><span className="kicker">ABOUT PIXPROMAX</span><h1>Practical tools for everyday files.</h1><p className="lede">PixProMax is a collection of browser-based image and PDF tools for common tasks such as compressing, resizing, converting, cropping, and preparing documents.</p><h2>Why it exists</h2><p>Simple file tasks should not require a complicated workflow. PixProMax focuses on clear controls, useful presets, and fast results without requiring an account for the active tools.</p><h2>How it works</h2><p>The active tools use your browser to process the files you select. This approach keeps the work close to your device and avoids creating an upload pipeline solely to provide everyday editing utilities.</p><h2>Tools available</h2><p>Current tool categories include {categories.join(", ")}. Each tool page explains its workflow, related tools, and any relevant limitations.</p><h2>Privacy approach</h2><p>We aim to be clear about what the site does. Claims about browser-side processing, optional services, and storage are described in the <a href="/privacy-policy">Privacy Policy</a> and should be read alongside the tools you use.</p><h2>Contact</h2><p>Have feedback, a bug report, or a privacy question? Visit <a href="/contact">Contact</a> for the current support options.</p></article>; }
