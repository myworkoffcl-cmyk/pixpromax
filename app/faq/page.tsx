import type { Metadata } from "next";
import { JsonLd } from "@/components/seo/json-ld";
const faqs = [["Is PixProMax free?", "The current browser-based tools are free, require no account, and do not add a PixProMax watermark."], ["Are my images uploaded?", "Active PixProMax tools process selected files in your browser. The current application has no file-upload API or server-side file-processing service."], ["What formats are supported?", "Core image tools support JPG, JPEG, PNG, and WebP. The converter also accepts AVIF, HEIC, and HEIF with JPG, PNG, or WebP output."], ["Does compression reduce quality?", "Compression can affect visual quality. PixProMax provides controls so you can choose a practical balance between size and appearance."], ["Can passport photos be accepted everywhere?", "No. PixProMax presets are convenience guides. Always check the current requirements of the receiving authority."], ["Does PixProMax require an account?", "No account is required for the current active tools."]];
export const metadata: Metadata = { title: "FAQ", description: "Answers to common questions about PixProMax browser-based image and PDF tools.", alternates: { canonical: "/faq" } };
export default function FaqPage() { return <article className="legal-page shell"><JsonLd data={{ "@context": "https://schema.org", "@type": "FAQPage", mainEntity: faqs.map(([name, text]) => ({ "@type": "Question", name, acceptedAnswer: { "@type": "Answer", text } })) }} /><span className="kicker">FAQ</span><h1>Questions, answered plainly.</h1><div className="faq-list">{faqs.map(([question, answer]) => <details key={question}><summary>{question}<span>+</span></summary><p>{answer}</p></details>)}</div></article>; }
